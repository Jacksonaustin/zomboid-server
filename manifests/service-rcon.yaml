apiVersion: v1
kind: Service
metadata:
  name: zomboid-rcon
  namespace: zomboid
  labels:
    app: zomboid-server
spec:
  type: ClusterIP
  selector:
    app: zomboid-server
  ports:
    - name: rcon
      protocol: TCP
      port: 27015
      targetPort: 27015